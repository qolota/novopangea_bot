var charMap = '.12345abcdefghijklmnopqrstuvwxyz';
/**
 * Convert from given Uint64 to a EOS's name format.
 * @param {String} value A string represents uint64
 * @returns
 */
exports.uint64ToName = function (value) {
    var str = [];
    var tmp = BigInt.asUintN(64, value);
    for (var i = 0; i <= 12; ++i) {
        var idx = tmp & BigInt(i === 0 ? 0x0f : 0x1f);
        str[12 - i] = charMap[Number(idx.toString())];
        tmp = tmp >> BigInt(i === 0 ? 4 : 5);
    }
    return str.join('').replace(/\.+$/g, '');
};
//# sourceMappingURL=uint64ToName.js.map